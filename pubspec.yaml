name: neon_fire
description: "프로해빗 - 운동 기록 관리 앱"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  #UI 라이브러리
  cupertino_icons: ^1.0.8

  # Firebase 라이브러리
  firebase_core: ^4.2.1 #초기화용
  firebase_auth: ^6.1.2 #인증용
  cloud_firestore: ^6.1.0  #데이터베이스용


  # 차트 라이브러리
  fl_chart: ^0.65.0  

  # 상태 관리
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:




  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
